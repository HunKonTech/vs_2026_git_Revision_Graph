# Plugin metadata (mirrors vscode/package.json + vs/source.extension.vsixmanifest).
pluginGroup=com.hunkontech.revgraph
pluginName=RevisionGraph
pluginVersion=0.1.0

# See https://plugins.jetbrains.com/docs/intellij/build-number-ranges.html
# DevEco Studio 5.x / NEXT is built on IntelliJ Platform ~231-233 (2023.1-2023.3).
# Kept low so the same plugin distribution installs across that whole range.
pluginSinceBuild=231
pluginUntilBuild=243.*

platformType=IC
platformVersion=2023.1.7

org.gradle.jvmargs=-Xmx2g
kotlin.stdlib.default.dependency=false
